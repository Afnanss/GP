import 'package:flutter/material.dart';
import 'package:k/NavBar.dart';
class Mapp extends StatelessWidget {
  const Mapp({Key? key}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
